'use strict';

/**
 * mission-section-heading router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::mission-section-heading.mission-section-heading');
