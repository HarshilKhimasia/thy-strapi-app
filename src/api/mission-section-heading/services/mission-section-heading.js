'use strict';

/**
 * mission-section-heading service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::mission-section-heading.mission-section-heading');
