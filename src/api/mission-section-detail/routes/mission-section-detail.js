'use strict';

/**
 * mission-section-detail router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::mission-section-detail.mission-section-detail');
