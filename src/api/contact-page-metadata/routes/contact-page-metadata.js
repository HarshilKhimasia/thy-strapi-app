'use strict';

/**
 * contact-page-metadata router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::contact-page-metadata.contact-page-metadata');
