'use strict';

/**
 * advance-filter-location router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::advance-filter-location.advance-filter-location');
