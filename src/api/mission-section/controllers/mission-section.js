'use strict';

/**
 * mission-section controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::mission-section.mission-section');
