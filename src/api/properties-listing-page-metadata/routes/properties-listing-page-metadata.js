'use strict';

/**
 * properties-listing-page-metadata router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::properties-listing-page-metadata.properties-listing-page-metadata');
