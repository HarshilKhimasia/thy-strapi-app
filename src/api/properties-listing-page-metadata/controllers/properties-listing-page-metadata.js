'use strict';

/**
 * properties-listing-page-metadata controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::properties-listing-page-metadata.properties-listing-page-metadata');
