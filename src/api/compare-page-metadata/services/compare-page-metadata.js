'use strict';

/**
 * compare-page-metadata service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::compare-page-metadata.compare-page-metadata');
