'use strict';

/**
 * compare-page-metadata controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::compare-page-metadata.compare-page-metadata');
